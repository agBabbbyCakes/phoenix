# Netlify configuration for Silverback
# The root index.html serves as the marketing/welcome page
# The entire repo is deployed, but index.html is the default landing page

[build]
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

